roles = ["frontend"]

[commands]
# La CLI esegue questo comando SENZA shell (niente espansione di $VAR né prefissi VAR=val).
# La porta assegnata dalla CLI arriva via env FRONTEND_PORT/PORT ed è letta in vite.config.ts.
# L'api-key di App Bridge arriva da VITE_SHOPIFY_API_KEY in .env.development.
dev = "npm run dev"
